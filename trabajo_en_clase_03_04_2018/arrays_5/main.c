#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numeros[CANT];
    int numAux;

    printf("ingrese numero a buscar");
    scanf("%d",&numAux);
    for(i=0;i<CANT;i++)
    {
        if(v[i]==numAux)
        {

        }
    }
}
