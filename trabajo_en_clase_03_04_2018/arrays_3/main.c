#include <stdio.h>
#include <stdlib.h>
#define CANT 5
int main()
{
    int estado[CANT];
    int i;
    int dato[CANT];
    int pocision[CANT];
    while(estado[i==0])
    {
        printf("ingrese dato: ");
        scanf("%d",&dato[i]);
        printf("ingrese posicion: ");
        scanf("%d",&pocision[i]);


    }
    printf("\n");
    return 0;
}
